package drafts;

import common.Common;
import common.Constant;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;
import page_objects.BookTicketPage;
import page_objects.HomePage;
import page_objects.LoginPage;
import tests.BaseTest;

import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

public class Draft {
//    public static void main(String[] args) {
//        DateTimeFormatter myFormatObj = DateTimeFormatter.ofPattern(Constant.FORMAT_DATE);
//        System.out.println(LocalDate.now());
//        System.out.println(LocalDate.parse(Common.plusDayFromNow(5)));
//        System.out.println(LocalDate.parse(Common.plusDayFromNow(5)).format(myFormatObj));
//    }
}